import 'package:flutter/material.dart';

class CalculateTimeScreen extends StatelessWidget {
  const CalculateTimeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Calculate Time'),
      ),
      body: const Center(
        child: Text('Calculate Time Screen - Phase 1 Placeholder'),
      ),
    );
  }
}
